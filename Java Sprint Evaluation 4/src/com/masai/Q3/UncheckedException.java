package com.masai.Q3;

public class UncheckedException extends RuntimeException{

	UncheckedException(){
		
	}
	
	//Unchecked exception created by extending the "RuntimeException";
	//These type of exception occurs at the time of the run Time;
	//It not handled by the compiler at the time of run time its handled;
	// programmer has to taken care for that to catch the exception by providing the 
	// proper try and the catch;
}
